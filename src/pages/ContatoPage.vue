<template>
    <main>
        <section class="container">
            <h1>Contato</h1>

            <section class="contacts">

                <div 
                    v-for="(contact, index) in contacts"
                    class="contact"
                    :key="index"
                >
                    <div class="contact-title">{{contact.title}}</div>
                    <router-link :to="contact.link" target="_blank">{{contact.label}}</router-link>
                </div>

            </section>
        </section>
    </main>
</template>


<script>
    import api from '@/services/api.js';


    export default {
        name: 'ContatoPage',
        data(){
            return {
                contacts: []
            }
        },
        mounted(){
            api.get('/contacts.json').then(response => {
                this.contacts = response.data
                //console.log(response.data);
            });
        }
    }
</script>

<style scoped>

    main {
        align-items: center;
    }

    .contacts {
        margin-bottom: 40px;
        color: var(--color-text-dark)
    }

    .contact-title {
        font-weight: 600;
        font-size: 18px;
    }

    .contact a {
        color: var(--color-text-dark)
    }

    .contact {
        margin-bottom: 20px;
    }



</style>